🎯 **What:**
The `ImagePropertyGetter.GetFileProperties` method explicitly catches `SixLabors.ImageSharp.UnknownImageFormatException` and `SixLabors.ImageSharp.InvalidImageContentException`. The previous tests were too generic, relying on catching any general `Exception` to verify logging behavior, leaving a gap in validating that these specific exceptions were handled as intended.

📊 **Coverage:**
- Replaced generic invalid/corrupt image tests with precise tests for `UnknownImageFormatException` and `InvalidImageContentException`.
- Verified that the `Serilog.ILogger` receives the exact exception types through explicit generic type matching (`It.IsAny<SixLabors.ImageSharp.UnknownImageFormatException>()` and `It.IsAny<SixLabors.ImageSharp.InvalidImageContentException>()`).
- Removed duplicated generic tests that asserted redundant behaviors.

✨ **Result:**
Increased testing reliability and strictness. The test suite now explicitly guarantees that `ImagePropertyGetter` correctly processes and logs specific ImageSharp exceptions, ensuring that unsupported formats and corrupted content are gracefully logged instead of throwing unhandled exceptions.
