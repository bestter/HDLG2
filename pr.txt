🧪 [testing improvement] Add coverage for InvalidImageContentException in ImagePropertyGetter

🎯 **What:** The `ImagePropertyGetter.GetFileProperties` method was mostly tested, but missing test coverage for corrupted image files that trigger `InvalidImageContentException`.

📊 **Coverage:**
- Added `ImagePropertyGetter_GetFileProperties_CorruptedImage_LogsWarningAndReturnsEmpty` to verify the handling of corrupted images.
- Updated `ImageSetup.cs` to generate `test_corrupted.jpg` which is a valid JPEG truncated halfway through.

✨ **Result:** Test coverage for `ImagePropertyGetter` is complete, ensuring that corrupted images are caught correctly via `InvalidImageContentException` instead of bubbling up generic exceptions or crashing.
